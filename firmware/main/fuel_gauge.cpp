#include "fuel_gauge.hpp"

// Intentionally empty until the optional MAX17048 hardware is present. Keeping
// this as a separate driver prevents battery-gauge code from leaking into the
// phone telemetry or UI.
